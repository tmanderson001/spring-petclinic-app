name: Deploy to Beta

on:
  push:
    branches:
      - beta
      
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Trigger Deployment
        run: |
          echo "Beta deployment triggered"
          echo "This will call shared workflows ac